import {LoginForm} from '../components';

// Пейджа для входа в профиль пользователя.

const LoginPage = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export {LoginPage};